name: cdn

environment:
  sdk: '>=2.0.0 <3.0.0'

dev_dependencies:
  build_runner: ^0.10.0
  build_test: ^0.10.2
  build_web_compilers: ^0.4.0

dependencies:
  # hooggaa dependancies
  googleapis: any
  googleapis_auth: any
